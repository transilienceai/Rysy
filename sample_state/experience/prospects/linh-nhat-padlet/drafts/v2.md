# Draft v2 — Linh Nhat

**Subject:** Arcade moderation without accounts

**Body:**

Sandbox shipped with content moderation as a top-priority engineering feature because user accounts gave you natural enforcement hooks. Arcade lands at a separate domain with no student login by design — same moderation problem, structurally harder because the policy-enforcement surface that Sandbox had isn't there.

Where it gets hard isn't the moderation decision itself — it's reconstructing what happened when something slips through, since without accounts the trace lives only across model inputs, outputs, and the request context around them.

Transilience treats that trace as something the platform produces continuously, not assembled when an incident review starts.

What does measurement look like for Arcade moderation right now?

— Transilience
The Security Operating System for the Cloud

---

**v1 → v2 changes (cross-draft witness):**

1. **CTA reframed to right altitude.** v1 asked "model-side logs only, or also platform-side correlation?" — SRE-plumbing question pitched at a frontend-mobile EM whose stated priority is product-level moderation. v2: "What does measurement look like for Arcade moderation right now?" — product-level, single open question, lets him answer at his own altitude.

2. **"Whatever the model returned" replaced** with "the request context around them" — drops the casual register that read as imprecision in an otherwise crafted email.

3. **Dropped "The gap that opens up"** — "the gap" was a diagnostic move appearing in three of four Padlet drafts. v2 opens that sentence with "Where it gets hard isn't..." — different syntactic move into the same observation.

4. **CTA is now a single open question (no em-dash binary).**

**Length:** 4 sentences | **Register:** warm-observational with engineering-pragmatic frame
