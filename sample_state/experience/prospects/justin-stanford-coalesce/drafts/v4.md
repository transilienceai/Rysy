# Draft v4 — Justin Stanford

**Subject:** IaC declared vs actual after two acquired stacks

**Body:**

Two acquired stacks absorbed in twelve months, multi-cloud Kubernetes at scale, the same small infra team — the gap between what Terraform declares and what the cloud actually looks like widens between audit cycles in ways that aren't visible until they accumulate.

Closing it is mostly a declare-the-baseline-and-watch-it-continuously problem, additive to the existing observability and CSPM stack rather than replacing any of it.

Transilience runs a continuous diff between Terraform-declared state and effective cloud state, with drift surfaced before it accumulates into audit findings.

Curious how your team currently handles drift detection across the acquired stacks — Terraform-only, or runtime checks on top?

— Transilience
The Security Operating System for the Cloud

---

**v3 → v4 change (driven by user-level feedback caught in cross-email diff):**

The four-item product line ("declared baseline, drift detection, prioritized findings, remediation human-in-the-loop") in v3 was the same phrase used in v2 of Satish and Mike — three of four Coalesce drafts shared the same product enumeration. That symmetry telegraphs templating-with-personalization-wrapper. Replaced with Justin's frame-specific language: "Transilience runs a continuous diff between Terraform-declared state and effective cloud state" — uses *diff* (engineer vocabulary), names Terraform specifically (his stack), and names what surfaces (drift before audit cycles compound it). Also added "and CSPM" to the additive-to-stack line in P2 to preempt the "we already have Wiz/Orca/Lacework" reply that was implicit but unaddressed in v3.

**Voice register:** dry-precise
**Length:** 4 sentences
