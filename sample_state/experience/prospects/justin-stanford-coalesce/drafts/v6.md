# Draft v6 — Justin Stanford

**Subject:** Terraform-declared vs actual cloud state

**Body:**

Two acquired stacks absorbed in twelve months, multi-cloud Kubernetes at scale, the same small infra team — the gap between what Terraform declares and what's actually deployed widens between audit cycles in ways that aren't visible until they accumulate.

Transilience surfaces this diff continuously — Terraform-declared vs. actual deployed state — before drift compounds.

How does your team currently handle drift detection across the acquired stacks — Terraform-only, or runtime checks on top?

— Transilience
The Security Operating System for the Cloud

---

**v5 → v6 changes (cross-draft witness):**

1. **Cut "across the surface" filler.** Witness flagged this as one abstraction swapped for another — "compounds" is the right verb on its own. v5: "with drift surfaced before it compounds across the surface" → v6: "before drift compounds." Three words, complete.

2. **Removed the "additive to existing CSPM and observability tooling" sentence** that echoed Satish's "additive to existing CSPM." Witness flagged this as a verbatim near-echo across two emails into the same 142-person org. The CTA ("Terraform-only, or runtime checks on top?") already implicitly engages the existing-tooling question by asking what he runs. The category-incumbent preemption now lives in the CTA shape rather than as an explicit positioning statement.

3. **Restructured product sentence to break the "[description] is what Transilience [verb]" structural echo with Satish v4.** v5: "Continuous diff between what Terraform says and what's actually running is what Transilience handles" (substance-first inversion, matched Satish's shape). v6: "Transilience surfaces this diff continuously — Terraform-declared vs. actual deployed state — before drift compounds" (subject-verb-object). Different syntactic shape.

**Length:** 3 sentences (down from 4 — paragraph 2's positioning content was redundant with the product sentence; cutting tightened the email)

**Voice register:** dry-precise
