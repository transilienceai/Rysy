# Draft v3 — Cyrille David

**Subject:** Coalesce Catalog and the controls cadence

**Body:**

Catalog metadata is the source of truth for which assets carry which policy. The IAM, Snowflake roles, and cloud-config that actually enforce those policies sit one layer down — and the gap between policy-in-catalog and control-in-cloud is invisible until a control turns out not to have been doing what the catalog implied.

Worth fifteen minutes if how that gap is closed today is something you've been working through?

— Transilience
The Security Operating System for the Cloud

---

**v2 → v3 change:** Witness flagged v2 as generic abstraction — "controls need to stay verified at the same cadence policies change" was a security truism that could be said about any governance product at any company; "Catalog" was functioning as a mail-merge variable. v3 anchors the observation in the specific operational mechanism: catalog tags assets, but the IAM/Snowflake-role/cloud-config layer that enforces those tags is a different surface that updates on its own cadence. The failure mode named — "a control turns out not to have been doing what the catalog implied" — is product-specific, not category-generic.

**Voice register:** dry-precise
**Length:** 3 sentences (the first sentence split for cadence; functionally one observation + one implication + one question)
**Architecture:** signal-to-question — observation about where catalog metadata lives vs where enforcement lives, the named failure mode, the question
**Trigger anchor:** Coalesce Catalog's public positioning as the governance layer of the platform; the operational reality that catalog-as-policy-source-of-truth is one architectural layer above the IAM/Snowflake-roles/cloud-config that actually enforce policies

**Drafting notes:**
- No Transilience product sentence — the email is short enough that the signature/sender does the disambiguation; adding a feature list at three sentences would be the same vendor-brochure failure v1 had
- No reference to Amaury's departure, his commit count, his GitHub stars, or other internal info per portrait guidance
- Specifically names IAM + Snowflake roles + cloud-config as the enforcement layer — these are real Coalesce-stack-relevant primitives (Snowflake-native product, multi-cloud deployment); not generic
- Failure mode is concrete: "a control turns out not to have been doing what the catalog implied" — this is the silent-failure pattern a hands-in-the-code engineering manager will recognize
- CTA reframed slightly to "if how that gap is closed today is something you've been working through" — invites him to share his current approach if he has one, rather than the more passive "if the question is one you've been working through"
- Distinct angle from the other three Coalesce drafts
